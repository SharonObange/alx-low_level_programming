#include "main.h"

/**
 * mul - multiply two numbers
 * @a: first integer
 * @b: second integer
 * Return: returns multiple of the two integers
*/

int mul(int a, int b)
{
	int multiple = a * b;
	return (multiple);
}
